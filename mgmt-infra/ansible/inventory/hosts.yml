# 管理ホストのインベントリ
# mgmt は自分自身なので local 接続を使用する(SSH を経由しない)
---
all:
  children:
    mgmt:
      hosts:
        localhost:
          ansible_connection: local
          ansible_python_interpreter: /usr/bin/python3
